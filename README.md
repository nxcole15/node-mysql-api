# node-mysql-api

A simple Node.js + MySQL API with user authentication, JWT authorization, and email verification.

## Features

- User registration and login
- JWT-protected routes
- Admin-only account listing
- Email verification and password reset support

## Run locally

```bash
npm install
npm start
```